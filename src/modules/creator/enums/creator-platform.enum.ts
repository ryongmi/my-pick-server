export enum PlatformType {
  YOUTUBE = 'youtube',
  TWITTER = 'twitter',
}

export enum VideoSyncStatus {
  NOT_SYNCED = 'not_synced',
  IN_PROGRESS = 'in_progress',
  SYNCED = 'synced',
  FAILED = 'failed',
}
